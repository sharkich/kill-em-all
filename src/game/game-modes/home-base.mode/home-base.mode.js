import GameMode from '../game.mode';

class HomeBaseGameMode extends GameMode {
    /**
     * Init Game mode
     */
    constructor () {
        super();
    }

    /**
     * Init game mode
     */
    init (...params) {
        super.init();
    }

    /**
     * Show game mode
     */
    show () {
        super.show();
    }

    /**
     * Destroy game mode
     */
    destroy () {
        super.destroy();
    }
}

export default HomeBaseGameMode;