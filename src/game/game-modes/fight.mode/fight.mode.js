import GameMode from '../game.mode';

class FightGameMode extends GameMode {
    /**
     * Init Game mode
     */
    constructor () {
        super();
    }

    /**
     * Show game mode
     */
    show () {
        super.show();
    }

    /**
     * Destroy game mode
     */
    destroy () {
        super.destroy();
    }
}

export default FightGameMode;